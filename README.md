# INSTALL GUIDE

    sudo gem install jekyll-import maruku safe_yaml
    sudo gem install jekyll -v 1.4.3
    npm install
    bundle install

# GET STARTED

    grunt serve

Voila!

# DEPLOY

    grunt deploy
